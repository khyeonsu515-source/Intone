// ─────────────────────────────────────────────
// AI에게 줄 지침(프롬프트) 생성 함수들
// ─────────────────────────────────────────────

/*
  buildArticleCheckPrompt: AI에게 "너는 뉴스 기사 판별기야"라고 역할을 부여하고
  어떤 형식으로 답해야 하는지 지정하는 지침 문자열을 반환합니다.
  백틱(`)으로 감싼 문자열은 여러 줄을 그대로 쓸 수 있는 템플릿 리터럴입니다.
  .trim()은 앞뒤 불필요한 줄바꿈을 제거합니다.
*/
function buildArticleCheckPrompt() {
  return `
너는 링크가 뉴스 기사인지 먼저 판별하는 분류기다.
제공된 URL, 링크 텍스트, title, meta, og:title, 본문 일부만 사용해 판단한다.
일반 쇼핑, 광고, 검색 결과, SNS 글, 영상 페이지, 게시판 목록, 언론사 홈, 카테고리 페이지는 뉴스 기사로 보지 않는다.
개별 사건이나 이슈를 다루는 기사 본문으로 보이면 뉴스 기사로 본다.
응답은 오직 JSON 객체 하나만 반환한다.

형식:
{
  "is_article": true,
  "confidence": 84,
  "reason": "개별 기사 제목과 본문 단락이 확인됨"
}
`.trim();
}

/*
  buildAnalysisPrompt: AI에게 신뢰도·어그로도 분석 기준과
  반환 형식을 상세하게 알려주는 지침 문자열을 반환합니다.
  각 항목의 배점 기준도 포함되어 있어 AI가 일관된 기준으로 채점하도록 유도합니다.
*/
function buildNewsSitePatternPrompt() {
  return `
너는 아직 등록되지 않은 사이트가 뉴스 전문 사이트인지 판단하는 분류기다.
제공된 host, 확인된 기사 URL 목록, 현재 URL, title, meta, 본문 일부만 근거로 사용한다.
뉴스 전문 사이트이거나 기사 페이지를 꾸준히 제공하는 언론/매체 사이트라면, 기사 URL을 식별할 수 있는 접두어를 알려 준다.
홈페이지 전체를 뜻하는 "https://example.com/" 같은 너무 넓은 접두어는 피한다.
"/news/", "/article/", "/view/", "/articles/"처럼 기사 페이지에 반복되는 경로 접두어를 우선한다.
확신이 낮거나 기사 URL 형식을 특정하기 어렵다면 is_news_site를 false로 반환한다.
응답은 오직 JSON 객체 하나만 반환한다.

형식:
{
  "is_news_site": true,
  "confidence": 86,
  "url_prefixes": [
    "https://example.com/news/",
    "https://example.com/article/"
  ],
  "reason": "확인된 URL들이 같은 매체의 개별 기사 페이지 형식과 일치함"
}
`.trim();
}

/*
  buildAnalysisPrompt: 신뢰도·어그로도 분석 프롬프트를 만듭니다.
  topic/core_keywords를 다루는 방식이 두 가지로 나뉩니다.

  ① resolvedTopic이 주어진 경우 (키워드 인덱스에서 이미 확실한 주제를 찾은
     경우): topic/core_keywords는 이미 정해졌으므로 AI에게 요청조차 하지
     않습니다. 프롬프트에서 이 필드들에 대한 설명과 JSON 예시를 아예 빼고,
     "이미 확인된 사건이다"라고만 알려줍니다. 대신 direction(이 기사만의
     관점)과 framing_keywords(이 기사만의 논조 키워드)는 기사마다 다를 수
     있으므로 항상 새로 판단하게 합니다.

  ② resolvedTopic이 없는 경우 (일치하는 키워드가 없거나, 여러 후보가 있어
     모호한 경우): 기존과 동일하게 topic/core_keywords를 AI가 직접
     만들게 하되, existingTopics가 있으면(모호한 후보들) 참고자료로 주고
     "같은 사건이면 그대로 재사용, 아니면 새로 만들어라"라고 지시합니다.
*/
function buildAnalysisPrompt({ existingTopics = [], resolvedTopic = null } = {}) {
  const scoringSections = `
[신뢰도 credibility_score: 0~100점]
- 출처 명확성: 20점
  기자명, 언론사, 작성일, 공식 출처 존재 여부
- 제목/본문 일치도: 25점
  제목이 본문 내용을 정확히 반영하는 정도
- 근거 충실도: 25점
  통계, 공식 발표, 전문가 인용, 자료 출처 존재 여부
- 표현 중립성: 15점
  감정적·선동적 표현이 적은 정도
- 맥락 제공성: 15점
  사건의 배경, 반대 의견, 한계, 추가 설명 제공 여부

[어그로도 clickbait_score: 0~100점]
높을수록 나쁨.
- 과장 표현: 20점
  "충격", "경악", "역대급", "난리", "소름" 같은 표현
- 궁금증 유도: 20점
  "알고 보니", "이유는?", "결과는?", "정체는?" 같은 표현
- 제목/본문 불일치: 25점
  제목이 본문보다 과장되거나 다른 인상을 주는 정도
- 감정 자극: 20점
  분노, 공포, 혐오, 불안, 논란을 과도하게 유도하는 정도
- 핵심 정보 은폐: 15점
  제목에서 중요한 주어·대상·결과를 숨기는 정도
`;

  const baseFields = `  "is_article": true,
  "credibility_score": 72,
  "clickbait_score": 38,
  "credibility_breakdown": {
    "source_clarity": 15,
    "title_body_match": 20,
    "evidence_quality": 18,
    "neutrality": 10,
    "context": 9
  },
  "clickbait_breakdown": {
    "exaggeration": 8,
    "curiosity_gap": 10,
    "title_body_mismatch": 7,
    "emotional_trigger": 8,
    "hidden_key_info": 5
  },
  "article_summary": "정부 발표에 따른 정책 변화와 관련 반응을 다룬 기사입니다.",
  "summary": "공식 자료 인용은 있으나 제목에 약간의 클릭 유도 표현이 있음",
  "warning": "제목만 보고 판단하지 말고 본문 근거를 확인하세요."`;

  const directionFields = `  "framing_keywords": ["부자 감세 논란", "형평성 훼손"],
  "direction": {
    "stance": "매우 부정적",
    "reason": "세제 개편이 다주택자에게 실질적 혜택을 주는 반면 서민 부담은 늘어난다는 점을 강조"
  }`;

  if (resolvedTopic?.topic) {
    const keywordList = Array.isArray(resolvedTopic.core_keywords) ? resolvedTopic.core_keywords : [];
    return `
너는 뉴스 링크의 신뢰도와 어그로도를 평가하는 분석기다.
이미 뉴스 기사로 판별된 링크만 입력된다.
반드시 사용자가 제공한 제목, URL, 메타 정보, 본문 일부만 근거로 삼아라.
정보가 부족하면 임의로 사실을 추정하지 말고 낮은 확신을 반영해라.
article_summary는 기사 내용을 1문장으로 짧게 요약해라.
응답은 설명 문장 없이 오직 JSON 객체 하나만 반환해라.
${scoringSections}
[이 기사의 주제는 이미 확인되었다 — topic/core_keywords는 요청하지 않는다]
이 기사는 데이터베이스에서 키워드가 겹치는 걸로 확인되어, 이미 다음과 같은
사건으로 확정되었다:
  topic: "${resolvedTopic.topic}"
  core_keywords: ${JSON.stringify(keywordList)}
이 두 값은 이미 확정되었으니 응답 JSON에 topic/core_keywords 필드를
포함하지 마라. 대신 아래 두 항목은 기사마다 다를 수 있으므로 이 기사
내용을 직접 보고 새로 판단해라.
- framing_keywords: 이 기사만의 논조/프레임을 드러내는 키워드 2~5개
- direction: 기사의 관점 { stance: "매우 부정적"|"약간 부정적"|"중립적"|"약간 긍정적"|"매우 긍정적", reason: 판단 근거 1~2문장 }

아래 형식과 키를 그대로 사용해라(topic/core_keywords는 포함하지 않는다).
{
${baseFields},
${directionFields}
}
`.trim();
  }

  const topicList = Array.isArray(existingTopics) ? existingTopics.filter((item) => item?.topic) : [];
  const existingTopicsSection = topicList.length
    ? `
[기존 주제 후보 — 데이터베이스에서 이 기사와 키워드가 겹치는 것으로 확인된 사건들이다]
아래 후보 중 이 기사와 "같은 사건/이슈"를 다루는 항목이 있으면, topic과
core_keywords를 항목에 있는 값 그대로 재사용해라(띄어쓰기 하나까지 똑같이,
새로 짓지 마라). 여러 후보가 있지만 이 기사와 다른 사건이라면 새로
만들어라. 후보:
${JSON.stringify(topicList, null, 2)}
`
    : `
[기존 주제 후보]
겹치는 키워드를 가진 기존 주제가 없다. topic/core_keywords를 새로 만들어라.
`;

  return `
너는 뉴스 링크의 신뢰도와 어그로도를 평가하는 분석기다.
이미 뉴스 기사로 판별된 링크만 입력된다.
반드시 사용자가 제공한 제목, URL, 메타 정보, 본문 일부만 근거로 삼아라.
정보가 부족하면 임의로 사실을 추정하지 말고 낮은 확신을 반영해라.
article_summary는 기사 내용을 1문장으로 짧게 요약해라.
응답은 설명 문장 없이 오직 JSON 객체 하나만 반환해라.
${scoringSections}
[주제/키워드 분류 — 같은 사건을 다룬 다른 기사와 나중에 매칭하기 위한 용도이며 화면에는 표시되지 않는다]
- topic: 기사가 다루는 핵심 주제를 한 문장(20자 내외)으로 요약
- core_keywords: 이 사건/이슈를 다른 기사와 매칭하기 위한 "논조 중립적" 고유 식별 키워드 3~6개.
  이 키워드들은 나중에 다른 기사 본문에 "그대로 등장하는지" 문자열로 대조해서 같은
  사건인지 판단하는 데 쓰인다. 흔한 단어가 섞이면 전혀 무관한 기사까지 같은 사건으로
  잘못 묶일 수 있으므로, 개수보다 "얼마나 이 사건에만 고유한가"가 훨씬 중요하다.
  반드시 다음 원칙을 따른다.
  · 정책명, 법안명, 기관명, 인물명, 지역명, 날짜/시기, 구체적 수치 등 "사실 그 자체"만 포함한다.
  · 긍정/부정 어느 쪽 기사든 동일하게 등장할 수밖에 없는 단어만 선택한다.
  · "특혜", "완화", "논란", "효과" 같은 평가성·감정성 표현은 절대 포함하지 않는다.
  · "정부", "발표", "정책", "대책", "논의", "국민", "경제", "오늘", "최근"처럼 다른 무관한
    사건의 기사에도 흔히 등장할 수 있는 일반적인 단어는 절대 포함하지 않는다 — 이
    키워드 하나만으로 검색했을 때 이 사건 이외의 기사는 거의 안 걸려야 한다.
  · 일반명사보다 고유명사(정확한 정책/법안명, 특정 기관명, 특정 인물명, 특정 지역명,
    구체적 수치·날짜)를 우선한다. 흔한 일반명사 하나로만 이루어진 키워드는 쓰지 않는다.
  · 좋은 예: "종합부동산세 개편안", "다주택자 세율", "2026년 7월", "국토교통부", "1주택자 특별공제"
  · 나쁜 예(너무 흔해서 무관한 기사와 겹칠 수 있음): "부동산", "세금", "정부 발표", "국민", "경제"
- framing_keywords: 이 기사만의 논조/프레임을 드러내는 키워드 2~5개 (예: "부자 감세", "시장 정상화", "형평성 논란" 등 — 여기는 논조 색깔이 있어도 된다)
- direction: 기사가 이 사건/주체를 부정적으로 그리는지 긍정적으로 그리는지, 단일
  스펙트럼으로 판단하는 객체
  · stance: 다음 5단계 중 하나 — "매우 부정적" | "약간 부정적" | "중립적" | "약간 긍정적" | "매우 긍정적"
    사건/주체를 명백히 비판하거나 부정적으로 그리면 "매우 부정적", 약한 우려나
    아쉬움 정도로만 드러내면 "약간 부정적", 찬반 어느 쪽에도 치우치지 않고
    사실 위주로 다루면 "중립적", 약하게 호의적이거나 옹호하는 정도면 "약간 긍정적",
    명백히 지지하거나 긍정적으로 그리면 "매우 긍정적"으로 판단한다.
  · reason: 왜 그 stance로 판단했는지 1~2문장 (summary/article_summary와 겹치지 않게, 논조 판단 근거만 적는다)
${existingTopicsSection}
아래 형식과 키를 그대로 사용해라.
{
${baseFields},
  "topic": "정부의 부동산 세제 개편안 발표",
  "core_keywords": ["종합부동산세 개편안", "다주택자 세율", "국토교통부", "2026년 7월 발표"],
${directionFields}
}
`.trim();
}

/*
  buildTopicCategoryPrompt: 새 topic이 처음 생성될 때 딱 한 번만 호출해서,
  이 사건을 고정된 대분류(category) 중 하나로 분류하게 하는 지침입니다.
  기존 topic에 기사가 합류할 때는 이미 정해진 category를 그대로 쓰고
  다시 묻지 않습니다(indexArticleTopic 참고) — 카테고리는 사건 자체의
  속성이라 기사가 늘어난다고 바뀌지 않기 때문입니다.
*/
function buildTopicCategoryPrompt() {
  return `
너는 뉴스 사건(topic)을 아래 고정된 카테고리 중 하나로 분류하는 분류기다.
사건의 대표 라벨과 핵심 키워드만 보고 가장 알맞은 카테고리 하나를 고른다.
반드시 아래 목록에 있는 값 중 하나만 그대로 사용한다. 새로운 카테고리 이름을 만들지 않는다.
애매하면 가장 근접한 카테고리를 고르고, 정말 어디에도 뚜렷이 안 맞을 때만 "기타"를 쓴다.
응답은 설명 문장 없이 오직 JSON 객체 하나만 반환한다.

카테고리 목록:
- 정치: 정부, 국회, 선거, 정당, 외교 정책, 공직자 관련
- 경제: 금융, 증시, 부동산, 세제, 기업 실적, 물가, 고용
- 사회: 사건사고, 재난, 범죄, 교육, 노동, 복지, 지역 이슈
- 국제: 해외 정치·경제·사회 이슈, 국가 간 관계
- 문화: 예술, 출판, 전시, 종교, 생활, 트렌드
- 과학기술: IT, AI, 우주, 의학·과학 연구, 신기술
- 스포츠: 각종 스포츠 경기, 선수, 리그, 대회
- 연예: 방송, 영화, 음악, 연예인 관련 소식
- 기타: 위 어디에도 뚜렷이 속하지 않는 경우

형식:
{
  "category": "경제"
}
`.trim();
}


